# 🚀 پروژه DevOps - فضاهای نام Container

## 📋 کلیات پروژه 
---

## 🎯 تمرین 1: بررسی Copy-on-Write (25 نمره)

### ماموریت شما:
بررسی کنید Copy-on-Write توی Docker چطور کار می‌کنه.

### کارهایی که باید انجام بدید:
1. **یه container بسازید و تست کنید**:
 ```bash
 docker run -it --name cow-test ubuntu:latest /bin/bash
 ```

2. **داخل container این کارها رو بکنید**:
 - 3 تا فایل با سایزهای مختلف بسازید
 - یه فایل سیستمی رو تغییر بدید
 - یکی از فایلاتون رو پاک کنید

3. **تغییرات رو مستند کنید**:
 - از `docker diff` استفاده کنید
 - با `docker inspect` سایز container رو چک کنید
 - از هر مرحله عکس بگیرید

### چی باید تحویل بدید:
- عکس از خروجی `docker diff`
- تحلیل اینکه چرا فایل پاک شده هنوز روی سایز تأثیر می‌ذاره
- توضیح مکانیزم CoW (300 کلمه)

---

## 🌐 تمرین 2: پروژه Network Namespace (25 نمره)

### ماموریت شما:
یه برنامه چند container بسازید که جداسازی شبکه رو نشون بده.

### کارهایی که باید انجام بدید:
1. **محیط رو راه‌اندازی کنید**:
 ```bash
 # شبکه سفارشی بسازید
 docker network create --driver bridge my-network
 
 # وب سرور اجرا کنید
 docker run -d --name web-server --network my-network nginx:alpine
 
 # کلاینت اجرا کنید
 docker run -it --name client --network my-network alpine:latest
 ```

2. **ارتباط شبکه رو تست کنید**:
 - ابزارهای شبکه رو توی کلاینت نصب کنید
 - اتصال بین containerها رو تست کنید
 - IP آدرس‌ها و routing رو مستند کنید

3. **با شبکه پیش‌فرض مقایسه کنید**:
 - همین containerها رو بدون شبکه سفارشی اجرا کنید
 - تفاوت‌ها رو مستند کنید

### چی باید تحویل بدید:
- نمودار توپولوژی شبکه
- عکس از تست‌های ping و تنظیمات IP
- جدول مقایسه شبکه سفارشی با پیش‌فرض

---

## 💾 تمرین 3: مدیریت Mount و Volume (25 نمره)

### ماموریت شما:
یه سناریو ماندگاری داده با استراتژی‌های مختلف mount بسازید.

### کارهایی که باید انجام بدید:
1. **انواع مختلف mount رو تست کنید**:
 ```bash
 # Bind mount
 mkdir -p /tmp/project-data
 docker run -it --name bind-test -v /tmp/project-data:/data ubuntu:latest
 
 # Named volume
 docker volume create project-volume
 docker run -it --name volume-test -v project-volume:/data ubuntu:latest
 
 # tmpfs mount
 docker run -it --name tmpfs-test --tmpfs /tmp ubuntu:latest
 ```

2. **یه سناریو واقعی بسازید**:
 - یه container دیتابیس با داده‌های پایدار شبیه‌سازی کنید
 - بقای داده بعد از restart/حذف container رو تست کنید
 - مجوزها و مالکیت فایل‌ها رو مستند کنید

### چی باید تحویل بدید:
- جدول مقایسه انواع mount
- عکس از تست‌های ماندگاری داده
- گزارش تحلیل مجوزهای فایل

---

## 🔒 تمرین 4: جداسازی Process و User (25 نمره)

### ماموریت شما:
جداسازی فضای نام process و user رو با پیامدهای امنیتی نشون بدید.

### کارهایی که باید انجام بدید:
1. **آزمایش جداسازی process**:
 ```bash
 # container با processهای محدود اجرا کنید
 docker run -d --name process-test ubuntu:latest sleep 3600
 
 # نمای processها رو مقایسه کنید
 docker exec process-test ps aux
 ps aux | grep sleep
 ```

2. **تست فضای نام کاربر**:
 ```bash
 # به عنوان کاربرهای مختلف اجرا کنید
 docker run -it --user 1001:1001 --name user-test ubuntu:latest
 
 # ساخت فایل و مجوزها رو تست کنید
 # پیامدهای امنیتی رو مستند کنید
 ```

3. **تحلیل امنیتی**:
 - تست کنید وقتی می‌خواید به processهای host دسترسی پیدا کنید چه اتفاقی می‌افته
 - تلاش‌های افزایش مجوز رو مستند کنید
 - مرزهای امنیتی رو تحلیل کنید

